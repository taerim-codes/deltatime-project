# 시간의흐름 (Delta Time) 출판사 웹사이트

## 프로젝트가 뭔가요

시간의흐름(deltatime.co)은 2018년 서울에서 시작한 1인 독립 출판사다. 공식 웹사이트가 없다.
이 프로젝트는 **팬이 만들어 출판사에 제안하는 웹사이트**다. 완성되면 배포 링크와 함께
대표(최선혜)에게 제안 메일을 보낼 예정이다.

품질 기준은 하나: **https://press.stripe.com** — 이 사이트의 폴리시 수준이 목표다.
클라이언트가 "이 정도 quality"라고 명시적으로 요구했다.

## 출판사에 대해 (디자인의 근거)

- 첫 책은 피아니스트 키스 자렛에 관한 『건반 위의 철학자』. 이 책을 내기 위해 회사가 만들어졌다.
  → 흑백 재즈, 쾰른 콘서트, 즉흥과 여백이 브랜드의 뿌리.
- 대표 시리즈 「말들의 흐름」: 끝말잇기 에세이 연작 10권.
  커피와 담배 → 담배와 영화 → 영화와 시 → 시와 산책 → 산책과 연애 → 연애와 술
  → 술과 농담 → 농담과 그림자 → 그림자와 새벽 → 새벽과 음악.
  이 체인 구조는 사이트 전체의 시그니처 모티프다 (푸터의 체인 라인 등).
- 기획력과 만듦새(종이, 제본, 판형)로 평가받는 출판사. 그래서 사이트도
  "책 = 물체(object)"라는 개념이 핵심이다. 썸네일 나열 금지.
- 인스타그램: @deltatime.co

## 지금까지 만든 것 (reference/deltatime-v7.html)

단일 HTML 프로토타입. 열어보면 전체 컨셉이 보인다. 구현된 것:

1. **스택 뷰** (다크 #14110E 배경): 책들이 눕혀진 3D 박스로 세로로 쌓임.
   - 정면 = 책등 (배경색은 각 표지에서 추출한 dominant color, books.json의 `spine`/`fg`)
   - 윗면 = 실제 표지를 90° 눕혀 rotateX(60°) 원근으로 보여줌
   - 책등 아래 종이 단면 7px, drop-shadow, 스크롤 리빌, hover 시 lift
   - 두께 로직: 키스 자렛 대형본 104px > 양장 82px > 반양장 70px > 시집 58px
2. **상세 뷰**: 책 클릭 → 페이지 전체가 그 책의 색으로 물듦 (Stripe 방식).
   왼쪽에 책이 기울어져 부유(bob 애니메이션), 오른쪽에 제목/저자/소개/구매 버튼 3줄/저자 섹션.
   흰 표지는 배경을 #F3F0E9로 치환 (pageBg 함수).
3. 카테고리 6개 서가: 말들의 흐름 / 시인선 / 소설 / 소리와 음악 / 카페 / 그 밖의 산문.

## 데이터 & 에셋

- `books.json`: 32권 전체. 실제 서지 데이터 (제목, 저자/역자, 출간일, 정가, 제본, 카테고리,
  spine 색, 표지 경로). 스크린샷에서 수집·검증된 실데이터임. 임의로 바꾸지 말 것.
- `assets/covers/*.png`: 표지 32장 (가로 284px). **저해상도 주의** — 서점 스크린샷에서
  추출한 것이라 큰 화면에서 확대하면 깨진다. 표지가 400px 이상으로 렌더되지 않게 할 것.
  최종본은 출판사에서 고해상 원본을 받아 교체할 예정 (제안의 대화 포인트이기도 함).
- 소개글: 『방밖에 없는 사람 방 밖에 없는 사람』만 실제 콘텐츠(`long`, `bio`, `extra` 필드).
  나머지는 한 줄 요약. 부제가 있는 책은 부제가 desc로 들어가 있음. 추후 채움.

## 디자인 시스템

- 색: 스택 배경 `#14110E` (웜 블랙) / 종이 `#FBFAF7` / 잉크 `#161514` / 스모크 `#8A857D`.
  책마다 spine 색 (books.json). 그 외 장식색 금지 — 색은 책에서만 나온다.
- 타이포: 고운바탕(Gowun Batang, 세리프·제목) / Noto Sans KR 300 (본문) /
  IBM Plex Mono (레이블·메타, letter-spacing 넓게). Google Fonts.
- 로고 표기: 워드마크 "시간의흐름" + "DELTA TIME" 모노 스몰캡, 책등엔 "ΔT".
- 톤: 여백 많고 조용함. 카피는 짧고 단정하게. 히어로: "천천히 만든 책은, 읽는 사람의 시간을 바꿉니다."
- 애니메이션 원칙: 적을수록 좋다. lift/fade/float 같은 단순 모션만. 각도가 꺾이는
  트랜지션은 이전 버전에서 어색하다고 반려됨. prefers-reduced-motion 필수 대응.

## 로드맵 (우선순위 순)

### P0 — 프로젝트화 + 스택 뷰 폴리싱
- Vite 프로젝트로 재구성 (vanilla JS 또는 취향껏 가볍게. 과한 프레임워크 금지).
- reference HTML의 로직을 컴포넌트/모듈로 분리, books.json + assets 연결.
- **dev 서버 띄우고 스크린샷을 찍어 직접 확인하며** 스택 뷰를 press.stripe.com과
  나란히 비교 튜닝: 윗면 원근각, 책등 조명 그라디언트, 그림자 깊이, 간격 리듬.
  스스로 보고 판단할 것 — 이게 이 프로젝트를 Code로 옮긴 이유다.

### P1 — 상세 뷰 완성
- 색 물드는 전환을 더 매끄럽게 (스택→상세 사이 공유 요소 전환 느낌, FLIP 시도 가능).
- 모바일 레이아웃 정교화.
- 구매 버튼에 실제 서점 URL 연결 (알라딘/교보/예스24에서 각 도서 검색해 링크 수집).

### P2 — WebGL 업그레이드 (선택, 시간 되면)
- Three.js로 스택 뷰 재구현: 천 텍스처, 실제 조명, 스크롤 연동 회전.
  Stripe가 이 방식이다. CSS 버전을 fallback으로 유지.

### P3 — 배포 + 제안
- Vercel 또는 Netlify 배포.
- README에 배포 링크 정리. (제안 메일 초안은 별도 작업.)

## 하지 말 것

- 표지 이미지 위에 필터/오버레이로 색 왜곡하기 (표지는 그대로 보여준다)
- 스톡 사진, 장식용 일러스트, 이모지
- 서점 리뷰 점수/판매 데이터 노출 (서점의 데이터임)
- 화려한 스크롤 하이재킹 — 스크롤은 자연스럽게

## 클라이언트(사용자) 메모

- 반말로 편하게 대화하는 걸 선호. 한국어.
- 피드백이 직설적임 ("애니메이션 이상함") — 방어하지 말고 스크린샷 찍어 확인 후 고칠 것.
- 이 출판사의 찐팬임. 디테일에 진심.
